module.exports = {
  semi: false, // 去分号
  singleQuote: true, // 去双引号
  // bracketSpacing: true, // json 对象加空格
  jsxSingleQuote: true, // jsx去双引号
  jsxBracketSameLine: true, // jsx “>” 不换行
  arrowParens: 'avoid', // (x) => x 变成 x => x
  htmlWhitespaceSensitivity: 'ignore' // 是否对空格敏感 <css|strict|ignore>
}
